name: main
on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 1 * *'
jobs:
  build:

    runs-on: ubuntu-latest
    steps:
      - name: Check out repo        
        uses: actions/checkout@v2
      - run: |
          ./sample.ps1
        shell: pwsh  
      
